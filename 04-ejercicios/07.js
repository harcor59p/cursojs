/**
 * Crear un algoritmo que devuelva
 * el previo del producto 
 * mas impuesto
 */

function precioCompleto(precio , impuesto) {
    return total = (precio + (precio * impuesto));
}

let resultado = precioCompleto(19.90 , 0.15);
console.log(resultado);